/**
 * Direction enum for entity movement and cell connections
 * Uses numeric values (0-3) for efficient array indexing
 */
export enum Direction {
  UP = 0,
  RIGHT = 1,
  DOWN = 2,
  LEFT = 3
}